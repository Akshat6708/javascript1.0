
//  objects inside arrays
// very usefull in real world applicaton

const users=[
    {userId:1, fullname:"akshat patidar", gander:"male"},
    {userId:2, fullname:"hariom patidar", gander:"male"},
    {userId:3, fullname:"aman patidar", gander:"male"},
]
for(let user of users){
    console.log(user.fullname);
}
// console.log(users);
// for(let )
